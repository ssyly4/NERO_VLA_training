[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "nero-vla-training"
version = "0.1.0"
description = "OpenPI training integration for NERO robot datasets"
requires-python = ">=3.10"
